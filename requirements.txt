# Docs build stack — sphinx-immaterial baseline with the HPE Graphik/DESIGN.md
# skin (see conf.py html_css_files + _static/hpe-*.css). Migrated off the legacy
# Sphinx 4 + sphinx_rtd_theme stack. Classic type->Enter search is restored via
# _templates/search.html + Sphinx's own stock search engine (see conf.py setup).
sphinx==8.2.3
sphinx-immaterial==0.13.9
myst-parser==5.1.0
sphinx-tabs==3.5.0
sphinx-notfound-page==1.1.0
sphinxcontrib-contentui==0.2.5
sphinxcontrib-images==1.0.1
docutils==0.21.2
sphinx-intl==2.3.2
